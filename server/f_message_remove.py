from f_auth_register import auth_register
# uncomment when edit_message is implemented
# from f_message_edit import edit_message

def remove_message(token, message_id):
    
    #sampleUserDict = auth_register("hwangyeji@gmail.com", message_id, "Daniel", "Yang")
    #sampleUserID = sampleUserDict["u_id"]
    #sampleMessageUserID = token
    
    # if message does not exist
    #if isinstance(message_id, int) == False:
        #raise ValueError(f"Error, message no longer exists")
    #if sampleMessageUserID != sampleUserID:
        #raise SystemError(f"Error, the message you are trying to remove does not belong to you")
    # add code here when edit_message is implemented
    # if ... :
    #    raise SystemError(f"...")
    
    
       
    return_dict = {}
    
    return return_dict
